meta {
  name: Collisions - List
  type: http
  seq: 2
}

get {
  url: {{baseURL}}/collisons?limit={{limit}}&offset={{offset}}
  body: none
  auth: inherit
}

params:query {
  limit: {{limit}}
  offset: {{offset}}
}

settings {
  encodeUrl: true
}
