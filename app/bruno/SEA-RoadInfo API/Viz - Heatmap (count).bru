meta {
  name: Viz - Heatmap (count)
  type: http
  seq: 15
}

get {
  url: {{baseURL}}/viz/collision-heatmap?metric=count
  body: none
  auth: inherit
}

params:query {
  metric: count
}

settings {
  encodeUrl: true
  timeout: 0
}
